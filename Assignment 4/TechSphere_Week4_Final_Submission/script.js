document.addEventListener('DOMContentLoaded',()=>{const file=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('[data-page]').forEach(a=>a.classList.toggle('active',a.dataset.page===file));document.querySelectorAll('.demo-form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const notice=document.getElementById('notice');if(notice){notice.textContent='Thanks! Your demo request has been received.';notice.classList.add('show');setTimeout(()=>notice.classList.remove('show'),3500)}form.reset()}));const params=new URLSearchParams(location.search);if(file==='contact.html'){const service=params.get('service');const pkg=params.get('package');const subject=document.getElementById('subject');if(subject&&(service||pkg)) subject.value=service||pkg;}});